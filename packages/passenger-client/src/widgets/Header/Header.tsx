const Header = () => {
  return (
    <header style={{ padding: '16px', background: 'var(--surface)', 'box-shadow': 'var(--shadow-sm)' }}>
      <h2>Header</h2>
    </header>
  );
};

export default Header;
